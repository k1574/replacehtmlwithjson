# JSON instead of HTML

Web is bloat, yes, but it has some good conceptions
that easily do the work we need to do.
HTML is the first problems on our way to the better software.
I suggest replacing HTML with simpler and faster JSON format.
The example of page is in the "example.json" file.

